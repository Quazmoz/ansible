---
- name: Gather System Facts
  hosts: all
  gather_facts: true
  tasks:
    - name: Print basic OS info
      ansible.builtin.debug:
        msg: "Host {{ inventory_hostname }} is running {{ ansible_distribution }} {{ ansible_distribution_version }} ({{ ansible_os_family }})"
